import { NextResponse } from "next/server"; import { requireUser,AuthError } from "@/lib/auth"; import { listProjects,createProject } from "@/lib/data/operations";
export async function GET(){try{const s=await requireUser();return NextResponse.json({projects:await listProjects(s.sub)})}catch(e){if(e instanceof AuthError)return NextResponse.json({error:e.message},{status:e.status});return NextResponse.json({error:"Error interno"},{status:500})}}
export async function POST(r:Request){try{const s=await requireUser();const b=await r.json() as {name?:string;description?:string};const name=(b.name??"").trim();if(!name)return NextResponse.json({error:"Nombre requerido"},{status:400});return NextResponse.json({project:await createProject(s.sub,name,b.description?.trim()||null)},{status:201})}catch(e){if(e instanceof AuthError)return NextResponse.json({error:e.message},{status:e.status});return NextResponse.json({error:"Error interno"},{status:500})}}
