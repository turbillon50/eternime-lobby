import { NextResponse } from "next/server"; import { requireUser,AuthError } from "@/lib/auth"; import { updateTask } from "@/lib/data/operations";
export async function PATCH(r:Request,{params}:{params:Promise<{id:string}>}){try{const s=await requireUser();const {id}=await params;const b=await r.json() as {status?:string;title?:string;dueAt?:string|null;priority?:number};const task=await updateTask(s.sub,id,b);return task?NextResponse.json({task}):NextResponse.json({error:"No encontrado"},{status:404})}catch(e){if(e instanceof AuthError)return NextResponse.json({error:e.message},{status:e.status});return NextResponse.json({error:"Error interno"},{status:500})}}
